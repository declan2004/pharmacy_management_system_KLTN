<IfModule mod_rewrite.c>
    RewriteEngine On
    # Chuyển mọi yêu cầu vào thư mục public
    RewriteRule ^$ public/index.php [L]
    RewriteCond %{REQUEST_URI} !^/public/
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>